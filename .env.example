# ==============================================================================
# ENVIRONMENT VARIABLES TEMPLATE
# ==============================================================================
# Copy this file to .env and fill in your values
# Never commit .env to version control
# ==============================================================================

# ------------------------------------------------------------------------------
# AUTHENTICATION (NextAuth.js)
# ------------------------------------------------------------------------------

# Required: Secret key for encrypting JWT tokens and session cookies
# Generate with: openssl rand -base64 32
# CRITICAL: This MUST be set in production or the app will fail to start
AUTH_SECRET=your-secret-here

# The base URL of your application
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXTAUTH_URL=http://localhost:3000

# ------------------------------------------------------------------------------
# UNIFORM CMS
# ------------------------------------------------------------------------------

# Required: Your Uniform project API key
# Get from: Uniform Dashboard → Settings → API Keys
UNIFORM_API_KEY=your-uniform-api-key

# Required: Your Uniform project ID
# Get from: Uniform Dashboard → Settings → Project Settings
UNIFORM_PROJECT_ID=your-uniform-project-id

# Required: Secret for Uniform Canvas preview/editing mode
# Generate with: openssl rand -base64 32
# Used to securely enable Canvas editing without requiring authentication
UNIFORM_PREVIEW_SECRET=your-uniform-preview-secret

# Data Residency: Uniform API endpoints
# US: https://uniform.app and https://uniform.global
# EU: https://eu.uniform.app and https://eu.uniform.global
# Choose based on your data residency requirements
UNIFORM_CLI_BASE_URL=https://uniform.app
UNIFORM_CLI_BASE_EDGE_URL=https://uniform.global
UNIFORM_API_HOST=https://uniform.app
UNIFORM_EDGE_API_HOST=https://uniform.global

# ------------------------------------------------------------------------------
# OAUTH PROVIDERS (Optional)
# ------------------------------------------------------------------------------

# GitHub OAuth
# Setup: https://github.com/settings/developers
# Callback URL: {NEXTAUTH_URL}/api/auth/callback/github
GITHUB_ID=your-github-oauth-client-id
GITHUB_SECRET=your-github-oauth-client-secret

# Google OAuth (if you add it)
# Setup: https://console.cloud.google.com/apis/credentials
# Callback URL: {NEXTAUTH_URL}/api/auth/callback/google
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret

# ------------------------------------------------------------------------------
# DEMO CREDENTIALS (Development Only)
# ------------------------------------------------------------------------------

# Optional: Demo user credentials (only used in development)
# Remove or leave empty in production
DEMO_USER=demo
DEMO_PASSWORD=password

# ------------------------------------------------------------------------------
# DATABASE (Optional - not currently used)
# ------------------------------------------------------------------------------

# If you switch from JWT to database sessions, add database URL here
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# ==============================================================================
# NOTES
# ==============================================================================
# 
# Required for Development:
# - AUTH_SECRET
# - UNIFORM_API_KEY
# - UNIFORM_PROJECT_ID
# - UNIFORM_PREVIEW_SECRET
#
# Optional (defaults to US):
# - UNIFORM_CLI_BASE_URL
# - UNIFORM_CLI_BASE_EDGE_URL
# - UNIFORM_API_HOST
# - UNIFORM_EDGE_API_HOST
#
# Required for Production:
# All of the above, plus:
# - NEXTAUTH_URL must be your production domain
# - OAuth credentials (GITHUB_ID, GITHUB_SECRET) if using OAuth
#
# Uniform Data Residency:
# - US region: uniform.app and uniform.global
# - EU region: eu.uniform.app and eu.uniform.global
# - See: https://docs.uniform.app for more information
#
# Security Best Practices:
# - Never commit .env to git (it's in .gitignore)
# - Use different secrets for dev/staging/production
# - Rotate secrets periodically
# - Use your platform's secret management (Vercel, AWS, etc.) in production
# ==============================================================================
